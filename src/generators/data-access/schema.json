{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "$id": "DataAccess",
  "title": "",
  "type": "object",
  "properties": {
    "basetypePath": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "pattern": "^[^@]*@[^@]*$",
      "x-prompt": "Input the path where the common generated typescript types should be places. Do not prepend with /libs or @<npm-scope>\n"
    },
    "npmScope": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 1
      },
      "pattern": "^[^@]*@[^@]*$",
      "x-prompt": "Input the npm scope, that can be used to access the common generated typescript types. Must include @\n"
    },
    "superDomainName": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 2
      },
      "pattern": "^(?!.*[/])(?!.*shared).*$",
      "x-prompt": "Input the name of the superDomain to which a data-access lib should be added. E.g. 'neurology'. Do not prepend with '/libs/'.\n"
    },
    "domainName": {
      "type": "string",
      "description": "",
      "$default": {
        "$source": "argv",
        "index": 3
      },
      "pattern": "^(?!.*[/])(?!.*shared).*$",
      "x-prompt": "Input the name of domain to which a data-access lib should be added. E.g. 'medicine'. Do not prepend with '/libs/' or superdomain name.\n"
    }
  },
  "required": ["basetypePath", "npmScope", "domainName", "superDomainName"]
}
